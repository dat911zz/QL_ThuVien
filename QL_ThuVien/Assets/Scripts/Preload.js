jQuery(function ($) {
    $(window).on('load', function () {
        $('.preload').fadeOut('slow');
        $('.precontainer').fadeOut('slow');
    });
})
$(document).ready(function () {
    $('.preload').fadeOut('slow');
    $('.precontainer').fadeOut('slow');
});
